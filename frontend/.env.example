# Backend API URL
# Should point to your local or deployed backend (including /api/v1)
VITE_BACKEND_API_BASEURL="http://localhost:3000/api/v1"

# AI Configuration (Optional)
# If you want to use Groq directly on the frontend (not recommended for production)
VITE_GROQ_API_KEY=""
